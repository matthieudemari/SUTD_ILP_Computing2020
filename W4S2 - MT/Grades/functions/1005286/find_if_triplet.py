def find_if_triplet(my_list):
    has_triplet=True
    Counter(my_list)
    print(Counter)
    
    if counter>=3:
        return ("has_triplet={}".format(has_triplet))
    else:
        return ("has_triplet={}".format(not has_triplet))

-------------------------------------------------------------------------------------


def find_if_triplet(my_list):
    counter = 0
    for item in my_list:
        if item == item_name:
            counter += 1
                if counter >=3:
                    return ("has_triplet={}".format(has_triplet))
                else:
                    return ("has_triplet={}".format(not has_triplet))