def are_all_consecutive(my_list):
    sorted_list = sorted(my_list)
    range_list = list(range(min(my_list), max(my_list)+1))
    boolean1 = sorted_list == range_list
    if boolean1:
        boolean1
    else:
        boolean1
    return boolean1